


const options = ['rust', 'abc']
const handle = 'rust'

const filtered = options.filter((el) =>  el === handle)

console.log(filtered);

const testArr = ['a', 'b', 'c', 'd']
const testStr = 'b'

const testFilter = testArr.filter((el) => el === testStr)
console.log(testFilter)